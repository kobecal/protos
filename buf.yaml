version: v2

modules:
  - path: proto

deps:
  - buf.build/googleapis/googleapis

lint:
  use:
    - STANDARD

breaking:
  use:
    - PACKAGE
  # TEMPORARY: pre-release redesign of the carparking contract (field type
  # changes and removals). Removed by the follow-up commit; do not extend.
  ignore:
    - proto/kobecal/contracts
    - proto/kobecal/miniprogram/carparking
